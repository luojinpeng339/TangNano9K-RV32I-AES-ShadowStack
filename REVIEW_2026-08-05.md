# Zkne AES CPU 复盘与 Day 2 预习

日期：2026-08-05
工程：`C:\Users\Administrator\Desktop\CPU`

## 一、今天完成到哪里

### 已完成并验证

1. 标准 RV32I 字段修正：
   - `rs1 = instr[19:15]`
   - `rs2 = instr[24:20]`
   - `rd  = instr[11:7]`
2. Zkne RV32 指令识别：
   - `aes32esi`
   - `aes32esmi`
   - `bs = instr[31:30]`
3. AES S-box：Python 数学生成 Verilog case 表，256 项完整。
4. `aes32_fu`：
   - byte select
   - S-box
   - ESI
   - ESMI partial MixColumns
   - rotate
   - XOR rs1
5. AES 控制信号穿过：
   - `id_stage`
   - `id_ex_reg`
   - `top_pipeline`
   - `ex_stage`
6. AES 结果复用 `aluresult_e`，经过 EX/MEM、MEM/WB、WB 写回。
7. 真实完整五级流水验证：
   - `aes32esmi x3,x1,x2,0` → `x3=A56363C6`
   - `aes32esi x4,x3,x2,1` → `x4=A56300C6`
8. 普通 RV32I 回归通过：
   - `addi/add/sw/lw/sub`
   - `x1=5,x2=7,x3=12,x4=12,x10=5`
9. 新增标准 RV32I：
   - XOR
   - SLL/SLLI
   - SRL/SRLI
10. `RotWord` 流水线验证：
    - `0C0D0E0F → 0D0E0F0C`
11. `SubWord(RotWord(w3))` 流水线验证：
    - `0D0E0F0C → D7AB76FE`

### 今天查出的并修复的 CPU 流水线问题

1. `data_mem` 同步读与 MEM/WB 采样时序不匹配：改为写同步、读组合。
2. load-use hazard 时，`EX/MEM` 不应使用 `stall_d`；否则 `lw` 被卡住。
3. `MEM/WB` 不应被 `flush_d` 清掉；老指令必须继续写回。
4. 寄存器堆增加 WB→ID 同周期 write-first bypass：避免 ID 在 WB 写回同一周期读到旧值。

这些修复不是 AES 特例，而是让原 CPU 的五级流水更正确。

## 二、必须掌握的核心代码

### 1. 标准 RISC-V 字段

```verilog
assign rs1_d = instr_d[19:15];
assign rs2_d = instr_d[24:20];
assign rd_d  = instr_d[11:7];
```

标准格式：

```text
31       25 24    20 19    15 14   12 11     7 6      0
+----------+--------+--------+-------+--------+--------+
| funct7   |  rs2   |  rs1   |funct3 |   rd   | opcode |
+----------+--------+--------+-------+--------+--------+
```

### 2. Zkne RV32 编码

```text
31       30 29      25 24    20 19    15 14   12 11     7 6      0
+----------+----------+--------+--------+-------+--------+--------+
| bs[1:0]  | funct5   |  rs2   |  rs1   |  000  |   rd   |  033   |
+----------+----------+--------+--------+-------+--------+--------+
```

```text
funct5 = 5'h11 → aes32esi
funct5 = 5'h13 → aes32esmi
bs      = instr[31:30]
```

控制器只比较：

```verilog
if ((funct3 == 3'b000) && (funct7[4:0] == 5'b10001)) begin
    crypto_en = 1'b1;
    crypto_op = 1'b0;
end
else if ((funct3 == 3'b000) && (funct7[4:0] == 5'b10011)) begin
    crypto_en = 1'b1;
    crypto_op = 1'b1;
end
```

不能比较整个 `funct7`，因为高两位是 `bs`。

### 3. `aes32_fu` 核心

```verilog
case (bs)
    2'b00: selected_byte = rs2[7:0];
    2'b01: selected_byte = rs2[15:8];
    2'b10: selected_byte = rs2[23:16];
    2'b11: selected_byte = rs2[31:24];
endcase
```

```verilog
assign mul2_sbox = xtime(sbox_out);
assign mul3_sbox = mul2_sbox ^ sbox_out;
assign esmi_mixed = {mul3_sbox, sbox_out, sbox_out, mul2_sbox};
```

```verilog
assign aes_result = rs1 ^ (crypto_op ? esmi_rotated : esi_rotated);
```

### 4. GF(2^8) 的 `xtime`

```verilog
function [7:0] xtime;
    input [7:0] value;
    begin
        if (value[7] == 1'b1)
            xtime = (value << 1) ^ 8'h1B;
        else
            xtime = value << 1;
    end
endfunction
```

数学含义：

```text
2*a = x*a mod (x^8+x^4+x^3+x+1)
```

模多项式为 `0x11B`；只保留低 8 位后的约减常量为 `0x1B`。

### 5. EX 的结果 MUX

```verilog
wire [31:0] alu_result_raw;
wire [31:0] aes_result;

alu u_alu (
    .srca(src_a_e),
    .srcb(src_b_e),
    .alu_control(alu_control_e),
    .aluresult(alu_result_raw),
    .zero(zero_e)
);

aes32_fu u_aes32_fu (
    .rs1(src_a_e),
    .rs2(pre_src_b_e),
    .bs(crypto_bs_e),
    .crypto_op(crypto_op_e),
    .aes_result(aes_result)
);

assign aluresult_e = crypto_en_e ? aes_result : alu_result_raw;
```

AES 的 `rs2` 必须接 `pre_src_b_e`，不能接可能被立即数替换的 `src_b_e`。

### 6. Forwarding 的核心

```verilog
assign src_a_e =
    (forward_a_e == 2'b10) ? alu_result_m :
    (forward_a_e == 2'b01) ? result_w : addr1_e;
```

AES 结果也走 `aluresult_e`，所以 hazard unit 不需要知道上一条是 ADD 还是 AES。

### 7. WB→ID 同周期旁路

```verilog
assign rdata1 = (addr1 == 0) ? {WIDTH{1'b0}} :
                ((write_en && (addr3 != 0) && (addr3 == addr1))
                 ? wdata : regs[addr1]);

assign rdata2 = (addr2 == 0) ? {WIDTH{1'b0}} :
                ((write_en && (addr3 != 0) && (addr3 == addr2))
                 ? wdata : regs[addr2]);
```

它解决的是：

```text
WB 正在写 x7，同时 ID 需要读 x7
```

## 三、AES S-box 数学复盘

```text
SBox(x) = affine_transform(GF(2^8) 中 x 的乘法逆元)
```

非零元素：

```text
x^-1 = x^254
```

仿射变换：

```text
y = a XOR rol8(a,1) XOR rol8(a,2)
      XOR rol8(a,3) XOR rol8(a,4) XOR 0x63
```

硬件当前不实时算逆元，而是：

```text
Python 数学公式 → 生成 256 项 Verilog case → FPGA 组合 LUT
```

所以 `aes_sbox.v` 是数学定义的硬件展开，不是手写魔法常数。

## 四、今天真正还没完成的部分

1. 第一轮 AES-128 round key 还没在 CPU 中跑通。
2. 还没验证 `Rcon` 注入。
3. 还没完成 `w4/w5/w6/w7` 链式生成。
4. 还没完成 K1→K10 全部密钥扩展。
5. 还没把 16-byte state 组织成可运行的 AES-128 加密程序。
6. 还没跑 FIPS-197 完整向量：

```text
key       = 000102030405060708090A0B0C0D0E0F
plaintext = 00112233445566778899AABBCCDDEEFF
cipher    = 69C4E0D86A7B0430D8CDB78070B4C55A
```

7. 还没做周期数、LUT、Fmax、上板结果。

## 五、明天施工路线

### Day 2-A：第一轮 key expansion

目标：验证：

```text
w0 = 00010203
w1 = 04050607
w2 = 08090A0B
w3 = 0C0D0E0F
```

```text
RotWord(w3) = 0D0E0F0C
SubWord     = D7AB76FE
Rcon(1)     = 01000000

`temp = D6AB76FE`
```

然后：

```text
w4 = w0 XOR temp = D6AA74FD
w5 = w1 XOR w4   = D2AF72FA
w6 = w2 XOR w5   = DAA678F1
w7 = w3 XOR w6   = D6AB76FE
```

第一轮 key：

```text
D6AA74FDD2AF72FADAA678F1D6AB76FE
```

施工顺序：

1. 用 `ADDI` 或程序常量提供 `Rcon=01000000`；
2. XOR 得到 temp；
3. 四次 XOR 生成 w4~w7；
4. 用完整流水线检查寄存器值；
5. 再考虑如何重复生成 K2~K10。

### Day 2-B：把 key expansion 做成可重复程序

标准 RV32I 当前够用的核心：

```text
XOR / OR / SLLI / SRLI / ADDI
```

可能还需要：

```text
LUI 或更完整的立即数装载方式
```

因为 `Rcon` 可以先用 `ADDI` 构造低 byte，或者用测试台预装寄存器；正式程序需要认真处理常量装载。

### Day 2-C：完整 AES-128

1. 先用 Python golden model 输出每轮 state/key；
2. CPU 程序逐轮执行 4 个 32-bit state word；
3. 中间轮用 `aes32esmi`；
4. 最终轮用 `aes32esi`；
5. 对拍 FIPS-197 密文。

## 六、今天的诚实进度

完整“RV32I + RV32 Zkne AES-128 CPU”约：

```text
功能实现：约 50% 左右
项目交付：约 35%～40%
```

已完成的是最难、最容易出流水线 bug 的第一半：

```text
官方指令 → 译码 → 流水线 → 前递 → AES EX → 写回
```

剩下主要是：

```text
key schedule 程序化
+ 10 轮组织
+ 完整向量验证
+ FPGA 资源/性能数据
+ 文档清理
```

## 七、晚上复盘建议

按这个顺序，不要一上来背 256 项 S-box：

1. 画五级流水：IF→ID→EX→MEM→WB；
2. 画 AES 指令从机器码到 `aluresult_e` 的线；
3. 手算 `A56363C6`；
4. 手算 `RotWord(0C0D0E0F)=0D0E0F0C`；
5. 手算四次 `aes32esi` 得 `D7AB76FE`；
6. 复习 `xtime(80)=1B` 为什么是 `0x1B`；
7. 复习 WB→ID bypass 为什么必要；
8. 预习第一轮 key：`D6AA74FDD2AF72FADAA678F1D6AB76FE`。
