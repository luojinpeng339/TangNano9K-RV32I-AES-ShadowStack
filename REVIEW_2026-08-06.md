# 2026-08-06 — AES-128 RTL / Zkne CPU 施工日结与晚间复习

工程目录：`C:\Users\Administrator\Desktop\CPU`

> 今日主题：从已接入五级流水 CPU 的 Zkne AES32 指令，扩展到可独立验证的 AES-128 组合 RTL 基础库；完成中间轮与最终轮的 FIPS-197 验证，并发布课程设计仓库 v1.0。

---

## 0. 今日最终状态

### 已验证的 AES RTL 模块

```text
aes_sbox.v
aes_subbytes.v
aes_shiftrows.v
aes_mixcolumn.v
aes_round_transform.v
aes_addroundkey.v
aes_middle_round.v
aes_final_round.v
```

### 已验证的关键结果

```text
K0 → K1 key expansion                    PASS（完整五级流水 CPU）
AES SubBytes                             PASS（259 项检查）
AES ShiftRows                            PASS
AES MixColumns                           PASS
AES RoundTransform                       PASS
AES middle round（第 1～9 轮结构）         PASS
AES final round（第 10 轮结构）            PASS
AES-128 Python FIPS-197 golden model     PASS
```

### 今天公开的 v1.0 仓库

```text
https://github.com/luojinpeng339/TangNano9K-Verilog-AES128
```

v1.0 公开范围：

```text
已验证 AES 基础 RTL + AES middle round + Python golden model + 自检 TB
```

注意：v1.0 README 已明确说明：

```text
不是完整 AES-128 encryptor
未做 Gowin 综合
未上 Tang Nano 9K 板
```

---

# Part A — 项目两条线必须分清

## A1. CPU / Zkne 指令线

目标：让五级流水 RV32I CPU 执行标准 RISC-V scalar crypto AES32 指令。

```text
IF → ID → EX → MEM → WB
```

当前已完成：

```text
aes32esi / aes32esmi 译码
控制信号穿过 ID/EX
EX 阶段 AES 功能单元
EX/MEM、MEM/WB、WB 写回
数据前递 forwarding
K0 → K1 key schedule 的完整五级流水连线测试
```

这条线的重点是：

```text
标准指令编码
流水线控制信号匹配
RAW 依赖与 forwarding
CPU 程序如何拼出 AES-128
```

## A2. 独立 AES RTL 数学线

目标：先把 AES-128 的数学数据通路拆成可独立验证的 Verilog 模块。

```text
128-bit State
→ SubBytes
→ ShiftRows
→ MixColumns
→ AddRoundKey
```

这条线的重点是：

```text
AES 数学正确
byte order 正确
GF(2^8) 正确
FIPS-197 中间值可对拍
```

最终两条线会汇合：

```text
独立 RTL 提供数学金标和模块级证据
CPU Zkne 路线用 aes32esi / aes32esmi 指令得到同一 FIPS-197 密文
```

---

# Part B — AES-128 的完整数学骨架

AES-128 一次处理：

```text
128-bit plaintext
128-bit key
```

标准 AES-128 总结构：

```text
Initial:
    AddRoundKey(K0)

Rounds 1–9:
    SubBytes
    ShiftRows
    MixColumns
    AddRoundKey(Kr)

Round 10:
    SubBytes
    ShiftRows
    AddRoundKey(K10)
```

最重要的例外：

```text
第 10 轮没有 MixColumns。
```

FIPS-197 标准向量：

```text
Key:
000102030405060708090A0B0C0D0E0F

Plaintext:
00112233445566778899AABBCCDDEEFF

Expected ciphertext:
69C4E0D86A7B0430D8CDB78070B4C55A
```

---

# Part C — State 的字节序：最容易犯错的部分

AES State 不是按普通字符串横向理解，而是 **column-major（列优先）**。

128-bit 输入：

```text
00112233445566778899AABBCCDDEEFF
```

对应矩阵：

```text
| 00 | 44 | 88 | CC |
| 11 | 55 | 99 | DD |
| 22 | 66 | AA | EE |
| 33 | 77 | BB | FF |
```

因此四个 32-bit 列为：

```text
col0 = 00112233
col1 = 44556677
col2 = 8899AABB
col3 = CCDDEEFF
```

在 RTL 中，对 32-bit column：

```text
a0 = in_col[31:24]
a1 = in_col[23:16]
a2 = in_col[15:8]
a3 = in_col[7:0]
```

这是 `aes_mixcolumn.v` 的固定约定。

---

# Part D — 初始 AddRoundKey

原始明文不能直接进入第一轮；必须先 XOR 初始密钥 K0：

```text
plaintext:
00112233445566778899AABBCCDDEEFF

K0:
000102030405060708090A0B0C0D0E0F

state0 = plaintext XOR K0:
00102030405060708090A0B0C0D0E0F0
```

数学上，AES 的 AddRoundKey 是 GF(2) 上的加法：

```text
a + b = a XOR b
```

不是普通整数加法：

```text
没有进位
没有 +
只有逐 bit XOR
```

RTL：`aes_addroundkey.v`

```verilog
assign out_state = state_in ^ round_key;
```

验证通过的第 1 轮 AddRoundKey：

```text
5F72641557F5BC92F7BE3B291DB9F91A
XOR D6AA74FDD2AF72FADAA678F1D6AB76FE
=   89D810E8855ACE682D1843D8CB128FE4
```

---

# Part E — SubBytes：AES 非线性来源

RTL：

```text
aes_sbox.v
aes_subbytes.v
```

## E1. 为什么 AES 必须有 S-box

如果 AES 只使用：

```text
XOR
ShiftRows
MixColumns
```

则整体仍是线性变换；攻击者可以用线性代数建立方程。

`SubBytes` 提供非线性（confusion）：

```text
out_byte = SBox(in_byte)
```

## E2. S-box 的数学定义

```text
SBox(x) = AffineTransform(x^(-1))
```

其中乘法逆元在有限域：

```text
GF(2^8)
```

中计算，模多项式是：

```text
x^8 + x^4 + x^3 + x + 1
= 0x11B
```

非零 x：

```text
x × x^(-1) = 1  (in GF(2^8))
```

零特殊定义：

```text
0^(-1) = 0
```

逆元之后再做仿射变换：

```text
y = a XOR rol8(a,1) XOR rol8(a,2)
      XOR rol8(a,3) XOR rol8(a,4) XOR 0x63
```

当前硬件实现策略：

```text
Python 根据数学定义生成 256 项 Verilog case 表
→ aes_sbox.v
```

不是在硬件中实时算求逆元。

## E3. 128-bit SubBytes 硬件映射

`aes_subbytes.v` 例化 16 个并行 S-box：

```text
byte0  → aes_sbox u0
byte1  → aes_sbox u1
...
byte15 → aes_sbox u15
```

性质：

```text
16 路没有数据依赖
纯组合逻辑
输入 byte 位置与输出 byte 位置不改变
```

第 1 轮输入/输出：

```text
00102030405060708090A0B0C0D0E0F0
        ↓ SubBytes
63CAB7040953D051CD60E0E7BA70E18C
```

验证：

```text
AES_SUBBYTES_TEST: PASS (259 checks)
```

其中包括：

```text
3 组定向 128-bit 向量
256 个 8-bit S-box 输入全覆盖
16 lane 同时检查
```

---

# Part F — ShiftRows：纯 byte 连线

RTL：

```text
aes_shiftrows.v
```

ShiftRows 不含：

```text
GF 乘法
加法
XOR
时钟
```

它只是静态 byte permutation。

规则：

```text
row 0: left rotate 0
row 1: left rotate 1
row 2: left rotate 2
row 3: left rotate 3
```

输入是 SubBytes 输出：

```text
63CAB7040953D051CD60E0E7BA70E18C
```

按列优先写成矩阵：

```text
| 63 | 09 | CD | BA |
| CA | 53 | 60 | 70 |
| B7 | D0 | E0 | E1 |
| 04 | 51 | E7 | 8C |
```

ShiftRows 后：

```text
| 63 | 09 | CD | BA |
| 53 | 60 | 70 | CA |
| E0 | E1 | 04 | D0 |
| 8C | B7 | 51 | E7 |
```

重新按列优先打包：

```text
6353E08C0960E104CD70B751BACAD0E7
```

核心 RTL 连线逻辑：

```verilog
assign out_state = {
    s00, s11, s22, s33,
    s01, s12, s23, s30,
    s02, s13, s20, s31,
    s03, s10, s21, s32
};
```

作用：

```text
把同一列的 byte 分散到不同列。
下一步 MixColumns 才能让影响跨列扩散。
```

验证：

```text
AES_SHIFTROWS_TEST: PASS
```

---

# Part G — MixColumns：GF(2^8) 矩阵乘法

RTL：

```text
aes_mixcolumn.v
```

每个模块处理一个 32-bit column：

```text
in_col = {a0, a1, a2, a3}
out_col = {b0, b1, b2, b3}
```

数学矩阵：

```text
| b0 |   | 02 03 01 01 |   | a0 |
| b1 | = | 01 02 03 01 | × | a1 |
| b2 |   | 01 01 02 03 |   | a2 |
| b3 |   | 03 01 01 02 |   | a3 |
```

展开为：

```text
b0 = 02·a0 XOR 03·a1 XOR a2 XOR a3
b1 = a0 XOR 02·a1 XOR 03·a2 XOR a3
b2 = a0 XOR a1 XOR 02·a2 XOR 03·a3
b3 = 03·a0 XOR a1 XOR a2 XOR 02·a3
```

## G1. `xtime` 的含义

```text
02·a = xtime(a)
03·a = xtime(a) XOR a
```

`xtime(a)` 表示有限域中乘以 x（也就是二进制意义的乘 2）后做模多项式约减：

```verilog
function [7:0] xtime;
    input [7:0] value;
    begin
        if (value[7])
            xtime = (value << 1) ^ 8'h1B;
        else
            xtime = value << 1;
    end
endfunction
```

为什么是 `8'h1B`：

```text
模多项式是 0x11B
最高 x^8 项溢出后，只对保留下来的低 8 位 XOR 0x1B
```

重要手算：

```text
xtime(80)
= (80 << 1) XOR 1B
= 00 XOR 1B
= 1B
```

## G2. 第 1 轮四列金标

```text
6353E08C → 5F726415
0960E104 → 57F5BC92
CD70B751 → F7BE3B29
BACAD0E7 → 1DB9F91A
```

拼接：

```text
5F72641557F5BC92F7BE3B291DB9F91A
```

验证：

```text
AES_MIXCOLUMN_TEST: PASS
```

---

# Part H — RoundTransform：把前三步串起来

RTL：

```text
aes_round_transform.v
```

组合逻辑数据流：

```text
in_state
  ↓
aes_subbytes
  ↓ sub_state
aes_shiftrows
  ↓ shift_state
four aes_mixcolumn instances in parallel
  ↓
out_state
```

数学式：

```text
RoundTransform(state)
= MixColumns(ShiftRows(SubBytes(state)))
```

第 1 轮例子：

```text
00102030405060708090A0B0C0D0E0F0
        ↓
5F72641557F5BC92F7BE3B291DB9F91A
```

验证：

```text
AES_ROUND_TRANSFORM_TEST: PASS
```

---

# Part I — AES middle round：第 1～9 轮

RTL：

```text
aes_middle_round.v
```

内部：

```text
aes_round_transform
        ↓
aes_addroundkey
```

数学：

```text
R(state, key)
= MixColumns(ShiftRows(SubBytes(state))) XOR key
```

第 1 轮：

```text
state0:
00102030405060708090A0B0C0D0E0F0

K1:
D6AA74FDD2AF72FADAA678F1D6AB76FE

R1:
89D810E8855ACE682D1843D8CB128FE4
```

验证：

```text
AES_MIDDLE_ROUND_TEST: PASS
```

---

# Part J — AES final round：第 10 轮

RTL：

```text
aes_final_round.v
```

数据流：

```text
state_in
  ↓
aes_subbytes
  ↓
aes_shiftrows
  ↓
aes_addroundkey(K10)
  ↓
ciphertext
```

数学：

```text
Final(state, K10)
= ShiftRows(SubBytes(state)) XOR K10
```

注意：

```text
最终轮没有 MixColumns。
```

FIPS-197 最终轮输入：

```text
R9:
BD6E7C3DF2B5779E0B61216E8B10B689

K10:
13111D7FE3944A17F307A78B4D2B30C5
```

经过最终轮输出：

```text
69C4E0D86A7B0430D8CDB78070B4C55A
```

验证：

```text
AES_FINAL_ROUND_TEST: PASS
```

当前注意：final round 已在主工程完成并通过独立测试，尚未纳入公开 v1.0（计划归入 v1.1 全 AES 链）。

---

# Part K — Key Schedule：K0 → K1 已通过 CPU 五级流水验证

AES-128 初始 key K0 拆为四个 32-bit word：

```text
w0 = 00010203
w1 = 04050607
w2 = 08090A0B
w3 = 0C0D0E0F
```

第一轮扩展：

```text
temp = SubWord(RotWord(w3)) XOR Rcon(1)

w4 = w0 XOR temp
w5 = w1 XOR w4
w6 = w2 XOR w5
w7 = w3 XOR w6
```

## K1 的详细金标

```text
RotWord(w3):
0C0D0E0F → 0D0E0F0C

SubWord(RotWord(w3)):
0D0E0F0C → D7AB76FE

Rcon(1):
01000000

temp:
D7AB76FE XOR 01000000 = D6AB76FE

w4:
00010203 XOR D6AB76FE = D6AA74FD

w5:
04050607 XOR D6AA74FD = D2AF72FA

w6:
08090A0B XOR D2AF72FA = DAA678F1

w7:
0C0D0E0F XOR DAA678F1 = D6AB76FE
```

因此：

```text
K1 = D6AA74FDD2AF72FADAA678F1D6AB76FE
```

## `aes32esi` 为什么能做 SubWord

`aes32esi` 每条只处理 `rs2` 的一个 byte：

```text
byte select (bs)
→ S-box
→ 放入对应位置
→ XOR rs1
```

四条连续指令累积：

```text
aes32esi x8, x0, x7, 0
aes32esi x8, x8, x7, 1
aes32esi x8, x8, x7, 2
aes32esi x8, x8, x7, 3
```

得到：

```text
x8 = D7AB76FE
```

这要求 CPU 的 forwarding 正确，因为后面三条立即读取前一条刚产生的 `x8`。

---

# Part L — CPU Zkne 复习

## L1. 标准 RV32 R-type 字段

```text
31       25 24    20 19    15 14   12 11     7 6      0
+----------+--------+--------+-------+--------+--------+
| funct7   |  rs2   |  rs1   |funct3 |   rd   | opcode |
+----------+--------+--------+-------+--------+--------+
```

```verilog
rs1 = instr[19:15];
rs2 = instr[24:20];
rd  = instr[11:7];
```

## L2. Zkne RV32 encoding 重点

```text
31       30 29      25 24    20 19    15 14   12 11     7 6      0
+----------+----------+--------+--------+-------+--------+--------+
| bs[1:0]  | funct5   |  rs2   |  rs1   |  000  |   rd   |  033   |
+----------+----------+--------+--------+-------+--------+--------+
```

```text
funct5 = 5'h11 → aes32esi
funct5 = 5'h13 → aes32esmi
bs      = instr[31:30]
```

不能比较完整 `funct7`，因为：

```text
funct7 的高 2 bit 是 bs
```

## L3. AES 功能单元在 EX 阶段

```text
ID: decode AES + read registers
        ↓
ID/EX: latch crypto_en / crypto_op / crypto_bs
        ↓
EX: aes32_fu
        ↓
EX/MEM → MEM/WB → WB
```

关键：AES rs2 必须使用尚未被立即数 MUX 替换的原始第二操作数：

```verilog
.rs2(pre_src_b_e)
```

而不是可能装着 imm 的：

```verilog
src_b_e
```

## L4. 今天之前已修复的流水线问题

```text
1. data_mem：同步读与 MEM/WB 采样不匹配
   修复：写同步，读组合。

2. load-use hazard 时 EX/MEM 不应使用 stall_d
   否则 lw 自己会被卡住。

3. MEM/WB 不应因为 flush_d 被清掉
   否则已进入后级的老指令无法写回。

4. Register file 加 WB→ID write-first bypass
   同一周期 WB 写 xN，ID 读 xN 时必须读到新值。
```

WB→ID bypass 核心：

```verilog
assign rdata1 = (addr1 == 0) ? 32'b0 :
                ((write_en && (addr3 != 0) && (addr3 == addr1))
                 ? wdata : regs[addr1]);
```

---

# Part M — 今天实际做过的工程修复

## M1. AES RTL 常见 Verilog 错误（已修）

今天碰到的错误几乎都是真实 RTL 常见失误：

```text
端口列表用 ; 而不是 ,
模块名拼写错误：aes_mixcolum / aes_subbyte / aes_shifrows
信号名不一致：state_in vs in_state，state_out vs out_state
wire 重复驱动 / 重复实例化
输出拼接 assign 缺 = 或缺 ;
子模块端口名对不上：sbox_in / in_sbox，out_state / state_out
中间信号拼写不一致：sub_statet、shift_stage、shift_state
```

核心经验：

```text
先让编译器通过语法和 elaboration。
再验证数学与 byte order。
不要看到仿真 FAIL 就立刻怀疑 AES 数学。
```

## M2. TB 也可能错

今天两次出现 TB 金标错误：

```text
1. ShiftRows 第四组预期手抄错误。
2. SubBytes 参考函数最初只覆盖 00–0F，导致 10–FF 误报。
```

经验：

```text
手抄十六进制容易错。
最终要以 Python golden model 自动生成/对拍为准。
```

---

# Part N — v1.0 开源与版本边界

发布目录：

```text
C:\Users\Administrator\Desktop\TangNano9K-Verilog-AES128
```

公开仓库：

```text
https://github.com/luojinpeng339/TangNano9K-Verilog-AES128
```

v1.0 tag：

```text
v1.0.0
```

已公开内容：

```text
rtl/     经过验证的 AES 基础模块和 middle round
tests/   自检 Icarus Verilog TB
tools/   Python FIPS-197 golden model
README.md
MIT LICENSE
```

v1.0 不包含：

```text
完整 AES 顶层
K0 → K10 硬件 key expansion
CPU 源码
Gowin 工程、约束、bitstream
资源/Fmax/上板数据
```

## 后续版本

```text
v1.1:
完整 AES-128 顶层
K0 → K10
9 middle rounds + final round
FIPS-197 最终密文验证

v2.0:
Gowin 综合
资源 / 时序 / Fmax
Tang Nano 9K 上板
可能的 UART / LED 演示

后续硬件安全线：
五级 RV32I Shadow Stack / CFI prototype
```

---

# Part O — 今晚复习清单

按顺序复习，别试图背 256 项 S-box 表。

## 必会 1：写出 AES 轮结构

```text
Initial: AddRoundKey(K0)

R1–R9:
SubBytes → ShiftRows → MixColumns → AddRoundKey

R10:
SubBytes → ShiftRows → AddRoundKey
```

并能回答：

```text
为什么 R10 没有 MixColumns？
答：FIPS-197 的标准定义；前九轮已完成充分扩散，最终轮保留非线性、行重排与最终密钥注入。
```

## 必会 2：画 State 的列优先布局

给出：

```text
00112233445566778899AABBCCDDEEFF
```

能画出：

```text
| 00 | 44 | 88 | CC |
| 11 | 55 | 99 | DD |
| 22 | 66 | AA | EE |
| 33 | 77 | BB | FF |
```

## 必会 3：解释 SubBytes 的作用

关键词：

```text
nonlinearity / confusion
GF(2^8) multiplicative inverse
affine transform
```

能回答：

```text
为什么不能只用 XOR、ShiftRows、MixColumns？
答：它们整体仍是线性系统；S-box 的有限域求逆打破线性关系。
```

## 必会 4：手推 ShiftRows

至少会：

```text
00112233445566778899AABBCCDDEEFF
→ 0055AAFF4499EE3388DD2277CC1166BB
```

并记住：

```text
ShiftRows 是 byte wiring，不是数值计算。
```

## 必会 5：手推 xtime

至少会：

```text
xtime(80) = 1B
```

并能解释：

```text
0x11B 是完整模多项式；8-bit RTL 约减时 XOR 0x1B。
```

## 必会 6：写 MixColumns 的第一行

```text
b0 = 02·a0 XOR 03·a1 XOR a2 XOR a3
```

并能说：

```text
03·a = xtime(a) XOR a
```

## 必会 7：说明 AddRoundKey 为什么是 XOR

```text
AES 中的 bitwise GF(2) 加法 = XOR
没有进位
```

## 必会 8：第一轮完整数据流

背这个主线即可：

```text
state0:
00102030405060708090A0B0C0D0E0F0

SubBytes:
63CAB7040953D051CD60E0E7BA70E18C

ShiftRows:
6353E08C0960E104CD70B751BACAD0E7

MixColumns:
5F72641557F5BC92F7BE3B291DB9F91A

XOR K1:
D6AA74FDD2AF72FADAA678F1D6AB76FE

R1:
89D810E8855ACE682D1843D8CB128FE4
```

## 必会 9：K0 → K1 的结构

```text
temp = SubWord(RotWord(w3)) XOR Rcon
w4 = w0 XOR temp
w5 = w1 XOR w4
w6 = w2 XOR w5
w7 = w3 XOR w6
```

金标：

```text
K1 = D6AA74FDD2AF72FADAA678F1D6AB76FE
```

## 必会 10：CPU 中为什么要 forwarding

能画：

```text
aes32esi x8, x0, x7, 0
aes32esi x8, x8, x7, 1
```

并说明：

```text
第二条进入 EX 时，x8 还没写回寄存器堆。
必须从 EX/MEM 或 MEM/WB 把上一条结果前递到 src_a_e。
```

---

# Part P — 明日施工入口

不要重新从模块开始写，顺序固定：

```text
1. 设计 / 实现 K0 → K10 key expansion
2. 做 aes128_encrypt_top.v
3. 初始 AddRoundKey(K0)
4. 串 9 个 aes_middle_round
5. 接 aes_final_round(K10)
6. 用 FIPS-197 完整密文验证
7. 把 v1.1 模块、TB、README 更新并 tag v1.1.0
8. 再进入 Gowin 综合和上板准备
```

v1.1 的唯一终点：

```text
00112233445566778899AABBCCDDEEFF
with key
000102030405060708090A0B0C0D0E0F
must produce
69C4E0D86A7B0430D8CDB78070B4C55A
```
